# Core
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-dotenv>=1.0,<2

# Data & persistence (SQLite built into Python — no extra driver for early dev)
sqlalchemy>=2.0,<3
pandas>=2.2,<3
numpy>=1.26,<3

# Phase 4 optional — uncomment when adding Postgres / Redis (see pyproject.toml extras)
# psycopg[binary]>=3.1,<4
# redis>=5.0,<6

# Optimization (v0 heuristics)
scipy>=1.12,<2

# CLI & utilities
click>=8.1,<9
structlog>=24.1,<25

# Dev / test
pytest>=8.0,<9
pytest-cov>=5.0,<6
ruff>=0.4,<1
mypy>=1.9,<2
